# API keys template — copy to secrets.yaml and fill in your keys
# secrets.yaml is git-ignored and will NEVER be committed

alpha_vantage:
  api_key: "YOUR_ALPHA_VANTAGE_KEY"

polygon:
  api_key: "YOUR_POLYGON_IO_KEY"

fred:
  api_key: "YOUR_FRED_API_KEY"
